{# filepath: /templates/user/new.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Create new User{% endblock %}

{% block body %}
    <h1>Create new User</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('app_user_index') }}">back to list</a>
{% endblock %}
