<form method="post" action="{{ path('app_team_task_delete', {'id': team_task.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ team_task.id) }}">
    <button class="btn">Delete</button>
</form>
