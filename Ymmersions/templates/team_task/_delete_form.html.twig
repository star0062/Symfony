<form method="post" action="{{ path('app_team_task_delete', {'task_id': team_task.task_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ team_task.task_id) }}">
    <button class="btn">Delete</button>
</form>
